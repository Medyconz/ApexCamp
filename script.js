document.querySelectorAll('.camp-form').forEach((form)=>{const message=form.querySelector('.form-message');form.addEventListener('submit',async(event)=>{if(!form.dataset.endpoint)return;event.preventDefault();if(!form.checkValidity()){form.reportValidity();if(message)message.textContent='Please complete the required fields.';return}const button=form.querySelector('button[type="submit"]');const oldLabel=button?button.textContent:'';if(button){button.disabled=true;button.textContent='Submitting...'}if(message)message.textContent='Sending your form...';const data={};new FormData(form).forEach((value,key)=>{if(data[key]){data[key]=Array.isArray(data[key])?data[key]:[data[key]];data[key].push(value)}else data[key]=value});try{const response=await fetch(form.dataset.endpoint,{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json'},body:JSON.stringify(data)});const result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.error||'The form could not be submitted.');form.reset();if(message)message.textContent=result.message||'Thank you. Your form was saved for the Apex Camp team.'}catch(error){if(message)message.textContent=error.message||'The backend is not reachable yet.'}finally{if(button){button.disabled=false;button.textContent=oldLabel}}})});