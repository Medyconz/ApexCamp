name = "apexcamp"
main = "worker.js"
compatibility_date = "2026-05-14"

# This makes the HTML/CSS/JS files in the repo available to env.ASSETS.
assets = { directory = ".", binding = "ASSETS" }

# Apex Camp database for registrations, applications, contact messages,
# editable registration dates, and merch products.
[[kv_namespaces]]
binding = "DB"
id = "a94018bdcb7143caaf496f6fd0e2cebe"

# Add ADMIN_TOKEN as a Worker secret in the Cloudflare dashboard.
