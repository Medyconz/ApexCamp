name = "apexcamp"
main = "worker.js"
compatibility_date = "2026-05-14"

# This makes the HTML/CSS/JS files in the repo available to env.ASSETS.
assets = { directory = ".", binding = "ASSETS" }

# After connecting this repo in Cloudflare, add these in the Worker dashboard:
# 1. Workers KV binding named DB
# 2. Secret named ADMIN_TOKEN
