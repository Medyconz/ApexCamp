name = "apexcamp"
main = "worker.js"
compatibility_date = "2026-05-14"

# Create a Workers KV namespace in Cloudflare and paste its id here.
# The binding name must stay DB because worker.js uses env.DB.
[[kv_namespaces]]
binding = "DB"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
